答：

```cpp
r1 = v2;	//合法。v2是顶层const，顶层const的拷贝不受限制。
p1 = p2;	//不合法。p2是底层const，p1是普通指针，二者不具有相同的底层const资格。
p2 = p1;	//合法。非常量可以转化为常量。
p1 = p3;	//不合法。p3是既是顶层const也是底层const，p1是普通指针。
p2 = p3;	//合法。p2和p3都是底层const，可以执行拷贝操作。
```

